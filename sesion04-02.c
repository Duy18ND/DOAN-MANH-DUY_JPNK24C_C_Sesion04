#include<stdio.h>
int main(){
	int age;
	printf("moi ban nhap so nguyen bat ky  ",age);
	 scanf("%d",&age);
	 if(age % 2 == 0 ){
	 	printf("%d la so chan",age); 
	 } else{
	 	printf("%d la so le",age); 
	 } 
	return 0; 
} 
